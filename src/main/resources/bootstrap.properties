#Add the configServer uri so the microservice can connect to it
#and load configurations
spring.cloud.config.uri=http://centralized-config:8080
#Specify that products.properties has priority over
# to load properties from
spring.cloud.config.name=location-saver
spring.config.import=optional:configserver:http://centralized-config:8080